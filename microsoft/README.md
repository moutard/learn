 Pour compiler sous linux placer vous dans le dossier.

 ```cmake . -G "Unix Makefiles"```
Cela produira un fichier make à partir du CMakeLists.txt

puis pour compiler:
```make```

enfin pour lancer les tests
```./runTests```


